import 'package:flutter/material.dart';

class TDatabase extends StatefulWidget {
  const TDatabase({super.key});

  @override
  State<TDatabase> createState() => _CDartState();
}

class _CDartState extends State<TDatabase> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
